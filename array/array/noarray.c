#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main()
{	//int i, a, j;
	//int h;
	//float m;

	//printf("3명의 점수를 입력하세요: ");
	//scanf("%d %d %d ", &i, &j, &a);

	//h = i + j + a;
	//printf("합계 : %d\n", h);

	//m = h / 3.0;
	//printf("평균 : %.1f", m);

	//int a, b, c, d, e, f, g, h, i, j;
	//int sum;
	//float ga;

	//printf("10명의 점수를 입력하세요:\n");
	//scanf("%d %d %d %d %d %d %d %d %d %d",
	//	&a, &b, &c, &d, &e, &f, &g, &h, &i, &j);

	//sum = a + b + c + d + e + f + g + h + i + j;

	//printf("합계 : %d\n", sum);

	//ga = sum / 10.0;
	//printf("평균 : %.1f\n", ga);
	int i;
	int j;
	int k, a, w;
	printf("물건 값과 투입 금액을 입력하세요 : ");
	for (i = 0;i <= 5000; i++) {
		printf("5000원 %d장", &k);
	};
	for (i = 0;i <= 1000; i++) {
		printf("1000원 %d장", &a);
	};
	for (i = 0;i <= 100; i++) {
		printf("100원 %d장", &w);
	};
	return 0;
}